#import "../../fragments/SeriesSummary.graphql"
#import "../../fragments/PageInfoFields.graphql"

query MySeries($filter: ListingFilterInput) {
  mySeries(filter: $filter) {
    nodes {
      ...SeriesSummary
    }
    pageInfo {
      ...PageInfoFields
    }
  }
}
