mutation UpdatePatreonCampaignSettings($teasersEnabled: Boolean!) {
  updatePatreonCampaignSettings(input: { teasersEnabled: $teasersEnabled }) {
    campaign {
      id
      teasersEnabled
    }
    errors
  }
}
