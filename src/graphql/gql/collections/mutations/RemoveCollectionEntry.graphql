mutation RemoveCollectionEntry($collectionId: ID!, $entryId: ID!) {
  removeCollectionEntry(input: { collectionId: $collectionId, entryId: $entryId }) {
    collection {
      id
    }
    errors
  }
}
