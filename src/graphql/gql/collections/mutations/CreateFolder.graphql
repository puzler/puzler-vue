mutation CreateFolder($name: String!, $parentId: ID) {
  createFolder(input: { name: $name, parentId: $parentId }) {
    folder {
      id
      name
      parentId
      puzzleCount
      collectionCount
    }
    errors
  }
}
