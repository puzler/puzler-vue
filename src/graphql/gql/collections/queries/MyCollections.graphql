#import "../../fragments/CollectionSummary.graphql"
#import "../../fragments/PageInfoFields.graphql"

query MyCollections($filter: ListingFilterInput) {
  myCollections(filter: $filter) {
    nodes {
      ...CollectionSummary
    }
    pageInfo {
      ...PageInfoFields
    }
  }
}
