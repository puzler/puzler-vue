query CollectionDetail($id: ID!) {
  collection(id: $id) {
    id
    title
    description
    visibility
    mode
    timed
    shareToken
    puzzles {
      id
      title
      status
      visibility
    }
  }
}
