#import "../../fragments/UserFields.graphql"

mutation UpdatePlayerPrefs($playerSettings: JSON, $colorPalette: JSON) {
  updatePlayerPrefs(input: { playerSettings: $playerSettings, colorPalette: $colorPalette }) {
    user {
      ...UserFields
    }
    errors
  }
}
