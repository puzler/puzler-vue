#import "../../fragments/UserFields.graphql"

mutation UpdateProfile($username: String, $displayName: String, $bio: String) {
  updateProfile(input: { username: $username, displayName: $displayName, bio: $bio }) {
    user {
      ...UserFields
    }
    errors
  }
}
