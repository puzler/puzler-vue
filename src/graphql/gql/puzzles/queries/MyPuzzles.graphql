query MyPuzzles($status: PuzzleStatusEnum) {
  myPuzzles(status: $status) {
    id
    title
    status
    visibility
    shareToken
    folder {
      id
      name
    }
    publishedVersion {
      id
      displayName
    }
  }
}
