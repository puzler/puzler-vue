#import "../../fragments/PuzzleAdminFields.graphql"

mutation RevokePuzzleAccess($puzzleId: ID!, $userId: ID!) {
  revokePuzzleAccess(input: { puzzleId: $puzzleId, userId: $userId }) {
    puzzle {
      ...PuzzleAdminFields
    }
    errors
  }
}
