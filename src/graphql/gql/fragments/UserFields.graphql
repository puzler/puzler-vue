#import "./UserThemeFields.graphql"

fragment UserFields on User {
  id
  email
  username
  displayName
  avatarUrl
  bio
  role
  passwordSet
  profileVisibility {
    solveHistory
    stats
    favorites
    subscriptions
    activity
  }
  playerSettings
  colorPalette
  onboardingSeen
  onboardingDisabled
  activeThemeId
  enableCustomStyles
  userThemes {
    ...UserThemeFields
  }
  oauthConnections {
    provider
    createdAt
  }
}
