mutation UpdateCollection($id: ID!, $title: String, $description: String, $visibility: CollectionVisibilityEnum, $mode: CollectionModeEnum, $timed: Boolean, $accentColor: CollectionAccentColorEnum, $bgTreatment: CollectionBgTreatmentEnum, $titleFont: CollectionTitleFontEnum) {
  updateCollection(
    input: { id: $id, attrs: { title: $title, description: $description, visibility: $visibility, mode: $mode, timed: $timed, accentColor: $accentColor, bgTreatment: $bgTreatment, titleFont: $titleFont } }
  ) {
    collection {
      id
      title
      description
      visibility
      mode
      timed
      accentColor
      bgTreatment
      titleFont
    }
    errors
  }
}
