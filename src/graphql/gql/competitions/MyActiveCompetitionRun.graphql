query MyActiveCompetitionRun {
  myActiveCompetitionRun {
    id
    secondsRemaining
    deadline
    collection {
      id
      title
      shareToken
      competitionConfig {
        submissionPolicy
        enforcedSettings
      }
    }
  }
}
