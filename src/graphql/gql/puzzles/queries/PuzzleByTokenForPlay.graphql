query PuzzleByTokenForPlay($token: String!) {
  puzzleByToken(token: $token) {
    id
    title
    authorName
    author {
      id
      username
      displayName
    }
    publishedVersion {
      id
      definition
      solutionHash
    }
  }
}
