#import "../../fragments/PageInfoFields.graphql"
#import "../../fragments/CollectionCardFields.graphql"

query Collections($filter: ListingFilterInput) {
  collections(filter: $filter) {
    nodes {
      ...CollectionCardFields
    }
    pageInfo {
      ...PageInfoFields
    }
  }
}
