query PuzzleForPlay($id: ID!) {
  puzzle(id: $id) {
    id
    title
    authorName
    author {
      id
      username
      displayName
    }
    publishedVersion {
      id
      definition
      solutionHash
    }
  }
}
