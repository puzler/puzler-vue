#import "../../fragments/PuzzleAdminFields.graphql"

mutation SetPuzzleVisibility($id: ID!, $visibility: PuzzleVisibilityEnum!) {
  setPuzzleVisibility(input: { id: $id, visibility: $visibility }) {
    puzzle {
      ...PuzzleAdminFields
    }
    errors
  }
}
