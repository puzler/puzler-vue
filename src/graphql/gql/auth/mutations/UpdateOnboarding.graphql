#import "../../fragments/UserFields.graphql"

mutation UpdateOnboarding($onboardingSeen: JSON, $onboardingDisabled: Boolean) {
  updateOnboarding(input: { onboardingSeen: $onboardingSeen, onboardingDisabled: $onboardingDisabled }) {
    user {
      ...UserFields
    }
    errors
  }
}
