#import "../../fragments/UserFields.graphql"

mutation DisconnectOauthProvider($provider: String!) {
  disconnectOauthProvider(input: { provider: $provider }) {
    user {
      ...UserFields
    }
    errors
  }
}
