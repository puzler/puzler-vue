mutation AddStoryPageToCollection($collectionId: ID!, $title: String) {
  addStoryPageToCollection(input: { collectionId: $collectionId, title: $title }) {
    storyPage {
      id
      title
      bodyHtml
    }
    collection {
      id
    }
    errors
  }
}
