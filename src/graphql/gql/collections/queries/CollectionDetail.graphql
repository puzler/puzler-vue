query CollectionDetail($id: ID!) {
  collection(id: $id) {
    id
    title
    description
    pageDescriptionHtml
    coverImageUrl
    accentColor
    bgTreatment
    titleFont
    visibility
    mode
    timed
    shareToken
    puzzles {
      id
      title
      status
      visibility
    }
  }
}
