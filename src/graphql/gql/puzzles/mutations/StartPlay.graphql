mutation StartPlay($puzzleId: ID!, $shareToken: String) {
  startPlay(input: { puzzleId: $puzzleId, shareToken: $shareToken }) {
    puzzlePlay {
      id
      cellState
      progressState
      timeElapsedSeconds
      isSolved
    }
    errors
  }
}
