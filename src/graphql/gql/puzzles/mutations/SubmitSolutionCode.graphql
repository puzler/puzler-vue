mutation SubmitSolutionCode($puzzleId: ID!, $code: String!, $shareToken: String) {
  submitSolutionCode(input: { puzzleId: $puzzleId, code: $code, shareToken: $shareToken }) {
    solved
    recorded
    solveMessage
    errors
  }
}
