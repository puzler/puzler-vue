#import "../../fragments/PageInfoFields.graphql"
#import "../../fragments/PuzzleCardFields.graphql"

query Puzzles($filter: ListingFilterInput) {
  puzzles(filter: $filter) {
    nodes {
      ...PuzzleCardFields
    }
    pageInfo {
      ...PageInfoFields
    }
  }
}
