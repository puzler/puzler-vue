VITE_API_URL=http://localhost:3000

# Cookieless Plausible analytics. Set to your site's domain (e.g. puzler.app)
# to enable in production builds only. Leave unset to disable entirely.
VITE_PLAUSIBLE_DOMAIN=
