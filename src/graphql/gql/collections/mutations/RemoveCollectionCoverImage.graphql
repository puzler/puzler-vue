mutation RemoveCollectionCoverImage($collectionId: ID!) {
  removeCollectionCoverImage(input: { collectionId: $collectionId }) {
    collection {
      id
      coverImageUrl
      coverThumbUrl
    }
    errors
  }
}
