#import "../../fragments/PageInfoFields.graphql"
#import "../../fragments/SeriesCardFields.graphql"

query PublicSeries($filter: ListingFilterInput) {
  publicSeries(filter: $filter) {
    nodes {
      ...SeriesCardFields
    }
    pageInfo {
      ...PageInfoFields
    }
  }
}
