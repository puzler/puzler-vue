#import "../../fragments/SeriesEntryFields.graphql"

query SeriesPublic($id: ID!) {
  series(id: $id) {
    id
    title
    description
    visibility
    subscribed
    subscriberCount
    author {
      id
      username
      displayName
    }
    entries {
      ...SeriesEntryFields
    }
  }
}
